# import Numpy

# there are many ways to import numpy, but the most common way is to import it
# as np

import numpy as np

# optionally you can import specific functions from numpy, for example:
# from numpy import array, arange, sin, cos, exp
# or
# from numpy import *
# import numpy


# import numpy as np is the most common way to import numpy, and it is
# recommended to use this convention in your code. This allows you to use
# the np prefix when calling numpy functions, which makes your code more
# readable and easier to understand.

num_array = np.array([1, 2, 3, 4, 5])

print(num_array)  # Output: [1 2 3 4 5]

# Simple enough so far, we have called the function "array" from "np " being
# the alias for library numpy. Numpy is a library that provides support for
# large, multi-dimensional arrays and matrices, along with a collection of
# mathematical functions to operate on these arrays. It is widely used in
# scientific computing, data analysis, and machine learning.
