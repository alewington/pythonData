# Python Data Analysis

# conda install numpy pandas scipy django matplotlib

# These can also be installed by using:
# pip install numpy pandas scipy django matplotlib

# update by using:
# conda update numpy pandas scipy django matplotlib
# or
# pip install --upgrade numpy pandas scipy django matplotlib

# This was set up in a virtual environment using conda. To activate the
# virtual environment, use the command:
# conda create --name myenv

# Activate by using:
# conda activate myenv

# To deactivate the virtual environment, use:
# conda deactivate

# This code is for data analysis using Python. It includes importing necessary
# libraries and setting up the environment for data analysis tasks.

# It is broken in to sections for better understanding and organization. Each
# section will cover different aspects of data analysis, including data
# manipulation, visualization, and statistical analysis.

# You will need to have a good grasp of Python programming and data analysis
# concepts to effectively use this code. Make sure to have the required
# libraries installed and the virtual environment activated before running
# the code.
# Please see Python Basics for a refresher on Python programming concepts.

# Ensure you have the modules and packages installed in your environment.

# You can check the installed packages by running:
"""bash
conda list
"""
# to view the list of installed packages in your conda environment.

# You can also use:
"""bash
pip list
"""
# to view the list of installed packages in your pip environment.

# and can import the necessary libraries in your Python script as follows when
# needed (not all at once):
"""python
import numpy as np
import seaborn as sns
import pandas as pd
import scipy
import matplotlib.pyplot as plt
import openpyxl
"""

# To install the packacges need can be found in the individual sections at the
# first lesson called "main.py".

# not to self: last last module update 26/07/2026
